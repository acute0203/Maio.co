const n=`---
title: 预测差多少才算准？把 MAE、RMSE、MAPE 一次讲透
category: ai-adoption-metrics
categoryLabel: AI 导入指标
difficulty: 入门
updated: 2026-07-18
description: 从需求预测、销量预测到供应链场景，讲清楚为什么回归任务不能看准确率，以及 MAE、RMSE、MAPE 各自的适用边界和常见陷阱。
---

[别再问「模型准不准」了：讲透精确率、召回率和 F1](/knowledge/precision-recall-f1) 讲的是分类任务——模型输出的是「是垃圾邮件 / 不是垃圾邮件」这种非黑即白的判断。但企业里还有一大类 AI 任务，输出的根本不是「是 / 不是」，而是一个具体的数字：明天卖几台、下周门店客流多少、这批物料成本是多少、机台温度会升到几度。

这类「预测一个连续数值」的任务，在机器学习里叫**回归（Regression）**。评估它好坏时，你会发现分类那套精确率、召回率完全用不上——因为它们的前提是「每个预测能判对或判错」，而在回归里，这个前提根本不成立。

这篇文章就来讲清楚：为什么回归任务要换一套指标，MAE、RMSE、MAPE 分别在衡量什么、各自的盲点在哪，以及面对不同的业务痛点，你到底该盯着哪个指标看。

## 为什么「准确率」在这里用不上

分类指标的算法，本质上是「把每一个预测判对还是判错，然后计数」——猜对的除以总数，就是准确率。这套逻辑成立的前提是：每个预测都有一个明确的「对」或「错」。

可一旦模型预测的是连续数字，「对」和「错」就没法定义了。假设模型预测明天卖 98 台，实际卖了 100 台。你说它错了吧，差 2 台已经相当准了，太苛刻；你说它对了吧，它又没真的命中 100。更麻烦的是，预测一个连续值想要「不多不少刚好命中」，概率几乎为零——你永远差那么一点。

所以在回归任务里，问题从「有没有猜对」变成了「差多少」。既然人人都差一点，那衡量的重点自然就落到差距的大小上。**RMSE 和 MAPE，就是两种衡量「误差到底有多大」的方式。** 在讲它们之前，得先搞清楚一个更基础的东西：误差本身。

## 地基：误差（残差）

一切的起点，是每一次预测的误差，也叫**残差（residual）**：

**误差 ＝ 预测值 − 真实值**

正数代表模型**高估**了（预测比实际大），负数代表**低估**了（预测比实际小）。

现在你可能会想：那把每次的误差平均一下，不就知道模型平均差多少了吗？——这里藏着第一个坑。误差有正有负，直接平均会**互相抵消**。假设你有两次预测，一次高估 100、一次低估 100，平均误差是 \`(+100) + (−100) = 0\`，看上去完美无缺，实际上两次都差了 100，糟糕得很。

所以，要衡量误差的真实大小，必须先「消掉符号」，只看差距、不看方向。消号有两条路：

- 取**绝对值**，把负号直接抹掉——这条路通向 MAE 和 MAPE。
- 取**平方**，负负得正——这条路通向 RMSE。

选哪条路，会带来完全不同的性格。

## MAE 平均绝对误差

最直接的消号方式，是给每个误差取绝对值，然后求平均，这就是**平均绝对误差（MAE）**：

$$
\\text{MAE} = \\frac{1}{n} \\sum_{i=1}^{n} \\left| \\hat{y}_i - y_i \\right|
$$

其中 $\\hat{y}_i$ 是模型的预测值，$y_i$ 是真实值，$n$ 是样本总数，$\\left| \\cdot \\right|$ 表示取绝对值。后面的公式都沿用这套记号。

MAE 回答的问题很朴素：**平均每次预测，差几个单位？** 如果你在预测销量，MAE = 8 就是「平均每天预测和实际差 8 台」。它的单位和预测目标完全一致（台、元、人、度），谁都看得懂，不需要额外解释。

MAE 还有一个重要性格：它对所有误差**一视同仁、线性对待**。差 10 就贡献 10，差 100 就贡献 100，不会因为某一次差得特别大就格外「记恨」。这让它对**离群值（outlier）比较稳健**——偶尔一两次异常大的误差，不会把整体指标带偏太多。

但这个「一视同仁」有时也是缺点。在有些业务里，「一次差得离谱」远比「很多次差一点点」危险得多——比如少备了一大批货直接停线。这种时候，MAE 太「淡定」了，它感知不到那一次致命的大误差。这就轮到 RMSE 出场了。

## RMSE 均方根误差

另一条消号的路是取平方，对应的指标是**均方根误差（RMSE）**。它的计算分三步：先把每个误差平方，再求平均，最后开平方根：

$$
\\text{RMSE} = \\sqrt{ \\frac{1}{n} \\sum_{i=1}^{n} \\left( \\hat{y}_i - y_i \\right)^2 }
$$

关键在「平方」这一步。平方会**不成比例地放大大误差**：误差从 10 变成 100（大 10 倍），平方后从 100 变成 10000（大了 100 倍）。也就是说，误差越大，被平方放大得越狠。这让 RMSE 对**少数几次大误差极度敏感**——只要出现一次严重失手，RMSE 就会明显往上蹿。最后那步开平方根，则是把单位从「平方后的单位」还原回原始单位，方便解读。

有一个恒成立的关系：**RMSE ≥ MAE**。两者的差距本身就是有用的信息：差距很小，说明误差比较均匀，都是些小差错；差距很大，说明存在个别「巨型误差」把 RMSE 拉高了。

RMSE 适合什么场景？答案是**「一次大失误就致命」的场景**：

- **电网负载预测**：一次严重的低估，可能导致供电不足、跳闸甚至大面积停电。你无法用「平时都挺准」来弥补这一次的灾难。
- **产能与库存规划**：一次大的缺货预判失误，可能让整条产线停摆。相比之下，日常的小偏差无关痛痒。

在这些场景里，你要的正是一个「对大误差极度敏感、会大声报警」的指标，RMSE 就是为此而生的。

## RMSE 的盲点

RMSE 很好，但它有一个致命的局限：**它带单位，因此不能跨尺度比较。**

「RMSE = 500」这个数字，脱离了业务规模就毫无意义。如果你在预测一家日销百万的大卖场，误差 500 简直神准；可如果你在预测一家日销 1000 的便利店，误差 500 就是灾难级的。同样是 500，含义天差地别。

由此带来一个现实问题：一家大卖场和一家便利店，它们的 RMSE **不能直接放在一起比**——数字大的不代表模型更差，很可能只是因为它的业务体量本来就大。

可企业里恰恰经常需要横向比较：不同产品线之间、不同区域门店之间，哪个模型预测得更好？或者你要向老板汇报，他想听的不是「差 500」，而是「差了百分之几」。这些需求，带单位的 RMSE 都满足不了。你需要一个**无单位的相对指标**——这就引出了 MAPE。

## MAPE 平均绝对百分比误差

**平均绝对百分比误差（MAPE）** 把每次的绝对误差除以真实值，换算成百分比，再求平均：

$$
\\text{MAPE} = \\frac{100\\%}{n} \\sum_{i=1}^{n} \\left| \\frac{\\hat{y}_i - y_i}{y_i} \\right|
$$

MAPE 的好处非常突出：它**无单位**，输出的是一个百分比。「平均误差 8%」这句话，老板一听就懂，不需要你解释业务规模。正因为它是相对值，**不同尺度的对象可以直接比较**——大卖场 6%、便利店 9%，一目了然。这也是为什么，MAPE 是需求预测、销量预测领域**最常被拿来汇报**的指标。

但也恰恰因为它太好用、太直觉，MAPE 也是**最容易被误用**的指标。它藏着两个不小的陷阱。

## MAPE 的两个陷阱

**陷阱一：真实值接近 0 时，MAPE 会爆炸甚至无定义。**

注意 MAPE 的分母是**真实值** $y_i$。如果某个真实值等于 0，就变成除以零，数学上直接无定义。而如果真实值只是很小（比如实际卖了 1 台），哪怕绝对误差只有 2 台，百分比误差也会飙到 200%，一个这样的点就能把整体 MAPE 灌爆，让指标失真。

所以，凡是**间歇性、低销量、含 0** 的数据序列，都不要用 MAPE。典型的比如新品（早期销量常为 0）、长尾商品、备品备件——这些场景里 MAPE 会给出完全误导性的数字。

**陷阱二：MAPE 的惩罚不对称，天然偏袒低估。**

还是因为分母是真实值。高估时，百分比误差可以突破 100%——预测 300、实际 100，误差百分比是 200%；但低估时，最多只能到 100%——哪怕预测 0、实际 100，误差百分比也就 100% 封顶。这意味着，在 MAPE 眼里，**高估比低估「更亏」**。

后果很严重：如果你把 MAPE 当成模型的优化目标，模型会学会**偏向低报**——因为低报的「罚分」上限更低。而如果你的业务其实**更怕缺货**（也就是低估的代价更高），你就等于亲手把模型优化到了错误的方向。

怎么办？可以考虑几个替代指标：

- **sMAPE（对称 MAPE）**：修正了高估低估惩罚不对称的问题。
- **WAPE / WMAPE（加权绝对百分比误差）**：按总量加权，对 0 值和低量场景更稳健，是零售和供应链里常用的选择。需要跨尺度比较时，WAPE 往往比 MAPE 更靠谱。

## 该用哪个：按业务痛点选的实操清单

理论讲完，落到实操，给你一份可以直接照着走的清单：

1. **先看误差的代价结构。** 如果「一次大失误」就足以致命（停电、停线、断供），选 **RMSE**——它对大误差最敏感。如果代价是线性的（差多少就损失多少），又想抵抗离群值的干扰，选 **MAE**。
2. **要跨产品/区域比较，或要跟老板讲百分比，用 MAPE。** 但用之前，先确认数据里**没有接近 0 的真实值**；一旦有，改用 **WAPE**。
3. **如果业务明显更怕低估**（缺货比滞销贵得多），**别单独用 MAPE**——它天然偏袒低估，会把模型带偏。这时应搭配 RMSE，或采用不对称的评估方式。
4. **设定明确的可接受阈值。** 别笼统地说「要准」，而要给硬指标，比如「MAPE ≤ 10%，且任何单日误差都不得超过 30%」。有门槛，验收才有依据。
5. **上线后持续监控。** 需求会随季节、促销、长期趋势不断**漂移**，今天准的模型几个月后可能悄悄失灵，指标监控要一直开着。

最后送你一句话。下次再有人拍胸脯说「我们预测准确率 95%」（多半指的是 MAPE 5%），你不动声色地反问一句：

> **「那真实值接近 0 的情况你们怎么处理？对我们这个业务来说，高估和低估，哪个更贵？」**

对方能不能利索地接住这两个问题，基本就能看出他是真懂回归评估，还是只背了个漂亮数字。
`;export{n as default};
