const n=`---
title: RMSE 與 MAPE：當 AI 預測的是「數字」，準確率就失靈了
description: 預測銷量、來客數、成本這類「數字」的模型，為什麼不能用準確率評估？從誤差出發，帶你搞懂 RMSE、MAPE 的差別、各自的盲點，以及該用哪一個。
category: ai-adoption-metrics
categoryLabel: AI 導入指標
difficulty: 入門
updated: 2026-07-18
---

在 [Precision、Recall、F1：看懂 AI 模型的準確度指標](/knowledge/precision-recall-f1) 中，我們談了三個評估指標。但它們有一個共同前提：模型輸出的是「類別」——是不是詐欺、是不是垃圾郵件、有沒有瑕疵。

可是企業要 AI 做的預測，很多時候根本不是「是或不是」，而是**一個數字**：明天會賣幾台、下週來客數多少、這批物料成本多少、三天後機台溫度幾度。這類「預測數字」的模型，統稱**迴歸（regression）**。而評估它們，分類那套準確率、召回率完全派不上用場。

這篇就從「為什麼準確率在這裡失效」講起，帶你認識評估數字預測的兩個主力指標——**RMSE** 與 **MAPE**。

## 1. 為什麼「準確率」在這裡派不上用場

準確率、Precision、Recall 這些指標，骨子裡都靠一件事運作：**把每個預測判成「對」或「錯」，然後數數量**。這在分類問題成立，因為答案本來就是離散的（是／不是）。

但預測數字時，「對或錯」這個概念**根本無法定義**。

假設模型預測明天賣 98 台，實際賣了 100 台。這算「對」還是「錯」？

- 說它「錯」，太苛刻——才差 2 台，幾乎神準。
- 說它「對」，也不對——它並沒有剛好命中 100。

而且連續數字幾乎**不可能剛好命中**：預測 100.0 而實際 100.0 的機率趨近於零。如果硬要用「完全命中才算對」的準確率，這個模型永遠是 0 分，毫無意義。

所以在迴歸問題裡，我們問的問題**從根本上換了**：不再問「有沒有猜對」，而是問「**差多少**」。我們不數對錯，而是量測「誤差的大小」。RMSE 和 MAPE，就是兩種量測誤差大小的方式。

## 2. 地基：誤差 = 預測值 − 實際值

任何一次數字預測，都會產生一個**誤差（residual，殘差）**，定義很單純：

**誤差 ＝ 預測值 − 實際值**

- 誤差為**正** → 高估（預測比實際大）
- 誤差為**負** → 低估（預測比實際小）

那最直覺的想法是：把每次的誤差平均一下，不就知道模型準不準了嗎？

**不行，而且會被騙得很慘。** 因為正負誤差會互相抵消。

舉個例子：模型第一天高估 100、第二天低估 100。直接平均：$(+100) + (-100) = 0$，平均誤差是 0，看起來「零誤差、完美無缺」。但事實上兩天各差了 100，這是一個爛到底的模型。

所以在算誤差之前，一定要先**消掉正負號**。消號有兩條路：

- 取**絕對值** → 長出 MAE 和 MAPE
- 取**平方** → 長出 RMSE

接下來的三個指標，差別幾乎全在「怎麼消號、怎麼加權」而已。

## 3. MAE（平均絕對誤差）：最直覺的量法

最樸素的做法，就是把每個誤差取絕對值，再平均。這叫 **MAE（Mean Absolute Error，平均絕對誤差）**。

$$
\\text{MAE} = \\frac{1}{n}\\sum_{i=1}^{n} \\left| \\hat{y}_i - y_i \\right|
$$

（其中 $\\hat{y}_i$ 是第 $i$ 筆的預測值、$y_i$ 是實際值、$n$ 是總筆數。）

意思非常好懂：**平均而言，每次預測差了幾個單位**。如果預測的是銷量，MAE = 5 就代表「平均每天差 5 台」。它的單位跟預測目標一樣（台、元、度），業務一聽就懂。

MAE 的關鍵特性是**一視同仁**：差 10 就記 10，差 100 就記 100，線性計算，不特別放大任何一種誤差。這讓它**對離群值比較穩健**——偶爾一次大暴走，不會過度影響整體分數。

但也正因為一視同仁，MAE 在某些情境不夠用——當「一次差很多」遠比「很多次差一點」危險時，我們需要一個會**特別懲罰大誤差**的指標。這就是 RMSE。

## 4. RMSE（均方根誤差）：當「大誤差」特別致命

RMSE（Root Mean Squared Error，均方根誤差）的做法是：把每個誤差**平方**、平均、再**開根號**。

$$
\\text{RMSE} = \\sqrt{\\frac{1}{n}\\sum_{i=1}^{n} \\left( \\hat{y}_i - y_i \\right)^2}
$$

拆開來看兩個關鍵動作：

- **平方**：這是重點。平方會**不成比例地放大大誤差**——差 10 平方後是 100，差 100 平方後是 10000。兩者原本差 10 倍，平方後差距被拉開成 100 倍。所以 RMSE 對「少數幾個很大的誤差」極度敏感。
- **開根號**：平方之後單位會變成「台²」這種怪東西，開根號是為了把單位變回原本的「台」，讓數值可以解讀。

一個實用的觀念：**RMSE 一定大於等於 MAE**。兩者差距越大，代表你的誤差分佈越不平均、藏著少數幾筆大偏差。所以同時看 RMSE 和 MAE，能幫你判斷「模型是平均地小差，還是偶爾大暴走」。

**什麼情境該用 RMSE？** 當一次大失誤的代價，遠高於多次小失誤時。例如：

- **電網負載預測**：一次嚴重低估就可能跳電，寧可容忍很多小誤差，也要壓住那個致命的大偏差。
- **產能與庫存規劃**：一次大缺貨導致整條產線停擺，損失遠超過平常的小誤差累積。

這些場景，你會希望模型「就算平常差一點沒關係，但絕對不能出現一次大災難」——這時就用 RMSE 當優化與驗收的指標。

## 5. RMSE 的盲點：它有「單位」，不能跨尺度比較

RMSE 很有用，但它有個先天限制：**數值是帶單位的**（500 元、30 台、2 度）。這帶來兩個問題。

第一，**單看一個 RMSE 數字沒有意義**。「RMSE = 500」到底好不好？完全要看規模：如果日銷額是一百萬，差 500 是神準；如果日銷額只有 1000，差 500 是災難。脫離了業務規模，這個數字無法解讀。

第二，**不能跨尺度互相比較**。你沒辦法拿「大賣場的 RMSE」直接跟「便利店的 RMSE」比高下，因為兩者的量級天差地遠。

所以，當你想**跨產品、跨區域、跨尺度比較**，或只是想跟老闆一句話講清楚「大概差幾 %」，就需要一個**相對比例**的指標——MAPE。

## 6. MAPE（平均絕對百分比誤差）：用「差幾 %」說話

MAPE（Mean Absolute Percentage Error，平均絕對百分比誤差）把每一筆誤差，都換算成「佔實際值的百分之幾」，再平均。

$$
\\text{MAPE} = \\frac{100\\%}{n}\\sum_{i=1}^{n} \\left| \\frac{\\hat{y}_i - y_i}{y_i} \\right|
$$

它的特性正好補上 RMSE 的盲點：

- **無單位、用百分比表達**。「平均偏差 8%」——不管業務、老闆還是客戶，一聽就懂，不需要先知道規模。
- **可跨尺度比較**。不管一天賣 100 台還是 10000 台，都化成百分比，就能公平地放在一起比。

正因為好溝通、可比較，MAPE 是**需求預測、銷售預測**這類專案最常被拿來報告的指標。但它也是**最容易被誤用、甚至反咬你一口**的指標——這點下一節必須講清楚。

## 7. MAPE 的兩個陷阱（不知道會被它坑）

MAPE 好用，但它有兩個結構性的問題，用之前一定要確認你的資料不會踩到。

**陷阱一：實際值接近 0 時，MAPE 會爆炸甚至無定義。**

MAPE 的分母是「實際值」。如果某天實際銷量是 0，就變成除以零，數學上無定義；如果實際值很小（例如某個冷門品項一天只賣 1 台），那麼只要差 1 台，就是 100% 的誤差，MAPE 會被這種低量的日子灌爆，整體數字失真。

**結論：間歇性、低銷量、或含有 0 的序列（新品、長尾商品、備品）不要用 MAPE。**

**陷阱二：懲罰不對稱，偏袒「低估」。**

因為分母是實際值，高估和低估在 MAPE 眼裡不對等：

- 高估時，誤差百分比**可以超過 100%**（預測 300、實際 100 → 200% 誤差）。
- 低估時，誤差百分比**最多只到 100%**（預測 0、實際 100 → 100% 誤差）。

結果是：如果你拿 MAPE 當優化目標，模型會漸漸學會**偏向低報**，因為低估的「罰分上限」比較低，這樣分數比較好看。但如果你的業務其實**更怕缺貨（低估比滯銷更貴）**，用 MAPE 反而會把模型優化到錯的方向去。

**替代方案**：想避開這些陷阱，可以看 **sMAPE**（對稱版本）或 **WAPE / WMAPE**（用總量加權，對 0 和低量穩健得多，零售與供應鏈很常用）。想跨尺度比較又怕 MAPE 出包，WAPE 通常是更穩的選擇。

## 8. 該用哪個？回到你的業務痛點

和評估分類模型時一樣，沒有「最好的指標」，只有「最適合你場景的指標」。收攏成一份實操清單：

1. **先問誤差的代價結構**：一次大失誤就致命（跳電、大缺貨）→ 用 **RMSE**，把大誤差壓下去；每筆誤差線性計價、又想抗離群 → 用 **MAE**。
2. **要跨產品／區域比較，或要對老闆講「%」** → 用 **MAPE**，但**務必先確認資料沒有接近 0 的值**；有的話，改用 **WAPE**。
3. **業務明顯怕低估（缺貨比滯銷貴）** → 不要單用 MAPE（它偏袒低估），搭配 RMSE 一起看，或改用不對稱的評估方式。
4. **設定可接受門檻**：例如「MAPE ≤ 10%，且沒有任何單日誤差 > 30%」——把兩種角度綁在一起，驗收才有依據，而不是憑一個平均數字說「還不錯」。
5. **上線後持續監控**：需求會隨季節、促銷、趨勢而漂移，今天準的模型三個月後可能就走鐘，指標要一直盯著。

---

**小結**：下次有人跟你說「我們的預測準確率 95%」（多半其實是指 MAPE 5%），你可以反問兩句——**「你怎麼處理實際值接近 0 的情況？對我們來說，是高估比較貴、還是低估比較貴？」** 答得出來，才是真的懂預測；答不出來，那個 95% 大概只是話術。
`;export{n as default};
